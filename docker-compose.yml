version: "3.9"

services:
  entryguardian:
    build: .
    container_name: entryguardian
    restart: unless-stopped

    env_file:
      - .env

    ports:
      - "127.0.0.1:8080:8080"

    volumes:
      - ./users.db:/app/users.db

    environment:
      - PYTHONUNBUFFERED=1
